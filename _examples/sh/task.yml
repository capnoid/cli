
name: Shell Echo
description: Echo a name using sh(1).
arguments: ["{{.name}}"]

# TODO(amir): The API requires a Dockerfile
# but we want to build an image locally, push it to
# the registry and run it :/
builder: docker

parameters:
  - name: Name
    slug: name
    type: string
