name: Node-JS Echo
slug: nodejs_echo
description: Echo a name using node.js.
arguments: ["{{.name}}"]
builder: node
builderConfig:
  entrypoint: main.js
  language: javascript
  nodeVersion: "15.11.0"

parameters:
  - name: Name
    slug: name
    type: string
